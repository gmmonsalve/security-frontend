const environment = {
    apiUrl: 'http://localhost:9000/api'
}

const routes  ={
    login: '/auth/login',
    register: '/auth/register',
    aes: '/AES/',
    userList: '/download'
}

export { 
    environment,
    routes
};