const environment = {
    apiUrl: 'http://localhost:9000/api'
}

const routes  ={
    login: '/auth/login',
    register: '',
    aes: '/AES/',
    userList: '/download'
}

export { 
    environment,
    routes
};